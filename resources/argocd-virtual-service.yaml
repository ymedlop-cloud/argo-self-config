apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: argocd-virtualservice
spec:
  gateways:
    - argocd-gateway
  hosts:
    - argo.docker-desktop.com
  http:
    - route:
      - destination:
          host: argocd-server
          port:
            number: 80